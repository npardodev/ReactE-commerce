import React from 'react';
import Grid from '@material-ui/core';

export const ItemDetailContainerStyle = theme => {

    return ({
        container: {
            /* En construccion */
        }

    })
};