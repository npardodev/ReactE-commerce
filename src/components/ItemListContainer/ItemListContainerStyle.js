import React from 'react';
import Grid from '@material-ui/core';

export const ItemListContainerStyle = theme => {

    return ({
        container: {
            /* En construccion */
        }

    })
};